{% extends 'base.html.twig' %}

{% block title %} Advance TestClock {% endblock %}

{% block body %}
    <div class="d-flex mt-3">
        <h1> Advance TestClock </h1>
        <a href="{{ path('app_test_clock_index') }}" class="btn ms-auto my-auto btn-primary"> Back </a>
    </div>
    <div class="row mb-3">
        <div class="col">
            {{ include('test_clock/_form.html.twig', {'button_label': 'Advance'}) }}
        </div>
    </div>

    {{ include('test_clock/_delete_form.html.twig') }}
{% endblock %}
