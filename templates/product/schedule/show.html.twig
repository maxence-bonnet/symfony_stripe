{% extends 'base.html.twig' %}

{% block title %} Schedule subscription {% endblock %}

{% block body %}
    <h1> {{ subscription.product.name }} </h1>

    <table class="table">
        {# <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subscription.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ subscription.product.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ subscription.product.description }}</td>
            </tr>
            <tr>
                <th>Stripe product Id</th>
                <td>{{ subscription.product.productId }}</td>
            </tr>
            {% for price in subscription.product.prices %}
                <tr>
                    <th>Prix : {{ price.type }}</th>
                    <td>{{ price.price }} € </td>
                    <td>id :{{ price.priceId }}</td>
                </tr>                
            {% endfor %}
        </tbody> #}
    </table>

    <a href="{{ path('app_subscription_index') }}"  class="btn btn-sm btn-primary"> Back </a>

    <a href="{{ path('app_subscription_schedule_edit', {'id': subscription.id}) }}" class="btn btn-sm btn-primary"> Edit </a>

    {{ include('subscription/_delete_form.html.twig') }}
{% endblock %}
